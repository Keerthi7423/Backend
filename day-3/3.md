# 🚀 Day 3 Example Project — Simple HTTP API

## 🎯 Project Goal

Build a basic Node.js HTTP server with:

* Multiple routes
* JSON API
* Proper responses

---

## 📁 Project Structure

```
day-3-server/
│
├── server.js
├── data.js
└── README.md
```

---

## 📄 1. server.js (Main Server)

```js
const http = require("http");
const users = require("./data");

const server = http.createServer((req, res) => {

  // HOME ROUTE
  if (req.url === "/" && req.method === "GET") {
    res.end("Welcome to My Server 🚀");
  }

  // ABOUT ROUTE
  else if (req.url === "/about" && req.method === "GET") {
    res.end("This is About Page");
  }

  // USERS API
  else if (req.url === "/users" && req.method === "GET") {
    res.setHeader("Content-Type", "application/json");
    res.end(JSON.stringify(users));
  }

  // 404 ROUTE
  else {
    res.statusCode = 404;
    res.end("404 Not Found ❌");
  }
});

server.listen(3000, () => {
  console.log("Server running on http://localhost:3000");
});
```

---

## 📄 2. data.js (Dummy Data)

```js
const users = [
  { id: 1, name: "Keerthi" },
  { id: 2, name: "Kumar" }
];

module.exports = users;
```

---

## 📄 3. README.md

```md
# Simple Node.js HTTP Server

## 🚀 Features
- Home route (/)
- About route (/about)
- Users API (/users)
- 404 handling

## ▶️ How to Run

1. Open terminal
2. Navigate to project folder
3. Run:
   node server.js

4. Open browser:
   http://localhost:3000

## 📡 API Endpoints

GET /
→ Welcome message

GET /about
→ About page

GET /users
→ Returns user data in JSON

## 🧠 Concepts Learned
- HTTP server
- Routing
- JSON response
- Status codes
```

---

## 🧠 Explanation (Simple)

### Flow:

```
Browser → Request → Server → Response
```

### Key Concepts:

* `http.createServer()` → creates server
* `req.url` → which route
* `req.method` → GET/POST
* `res.end()` → send response

---

## 🧪 Test URLs

* [http://localhost:3000/](http://localhost:3000/)
* [http://localhost:3000/about](http://localhost:3000/about)
* [http://localhost:3000/users](http://localhost:3000/users)

---

## 🔥 What You Learned

* How server works
* Routing manually
* Sending JSON data
* Handling errors (404)

---

## 🚀 Next Upgrade Ideas

* Add POST route
* Add dynamic routing (/users/1)
* Move to Express.js (Day 4)

---

🔥 This project = Your FIRST real backend server
