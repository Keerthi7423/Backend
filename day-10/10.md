DAY 10 — EMAIL SENDING + OTP VERIFICATION + PASSWORD RESET

---

## 🎯 GOAL

* Learn email sending from backend
* Generate OTP
* Verify OTP
* Reset password
* Understand real authentication flow

Real examples:

* Gmail → OTP verification
* Instagram → Reset password
* Banking apps → Secure login verification

---

## 🧠 WHAT IS OTP?

OTP = One Time Password

Example:

```bash
483921
```

Properties:

✔ Used only once
✔ Expires after some time
✔ Used for security

---

## REAL FLOW

User clicks:

Forgot Password

↓

Enter Email

↓

Backend generates OTP

↓

Email sent

↓

User enters OTP

↓

OTP verified

↓

Reset password

---

## 📦 INSTALL PACKAGES

```bash
npm init -y

npm install express nodemailer bcryptjs
```

Package usage:

express
→ server

nodemailer
→ send emails

bcryptjs
→ hash password

---

## 📁 PROJECT STRUCTURE

```bash
day-10-email-otp/
│
├── data/
│   └── users.json
│
├── controllers/
│   └── authController.js
│
├── routes/
│   └── authRoutes.js
│
├── server.js
├── package.json
└── .env
```

---

## .env FILE

```env
EMAIL_USER=your_email@gmail.com

EMAIL_PASS=your_app_password
```

⚠ Important:

Do NOT use Gmail password directly

Use App Password

---

## server.js

```js
const express = require("express");
const authRoutes = require("./routes/authRoutes");
require("dotenv").config();

const app = express();

app.use(express.json());

app.use("/api/auth", authRoutes);

app.listen(3000, () => {
  console.log("Server running on port 3000");
});
```

---

## controllers/authController.js

```js
const fs = require("fs");
const bcrypt = require("bcryptjs");
const nodemailer = require("nodemailer");


// SEND OTP
const sendOTP = async (req,res)=>{

const {email}=req.body;

const data=fs.readFileSync(
"data/users.json",
"utf-8"
);

let users=JSON.parse(data);

const user=users.find(
u=>u.email===email
);

if(!user){
return res.status(404).json({
message:"User not found"
});
}


const otp=Math.floor(
100000+Math.random()*900000
);


user.otp=otp;

fs.writeFileSync(
"data/users.json",
JSON.stringify(users,null,2)
);


const transporter=
nodemailer.createTransport({

service:"gmail",

auth:{
user:process.env.EMAIL_USER,
pass:process.env.EMAIL_PASS
}

});


await transporter.sendMail({

from:process.env.EMAIL_USER,

to:email,

subject:"OTP Verification",

text:`Your OTP is ${otp}`

});


res.json({
message:"OTP sent successfully"
});

};


// VERIFY OTP

const verifyOTP=(req,res)=>{

const {email,otp}=req.body;

const data=fs.readFileSync(
"data/users.json",
"utf-8"
);

const users=JSON.parse(data);

const user=users.find(
u=>u.email===email
);

if(user.otp!=otp){

return res.status(400).json({
message:"Invalid OTP"
});

}

res.json({
message:"OTP verified"
});

};


// RESET PASSWORD

const resetPassword=async(req,res)=>{

const {
email,
newPassword
}=req.body;


const data=fs.readFileSync(
"data/users.json",
"utf-8"
);

let users=JSON.parse(data);


const user=users.find(
u=>u.email===email
);


const hashedPassword=
await bcrypt.hash(
newPassword,
10
);


user.password=
hashedPassword;


delete user.otp;


fs.writeFileSync(
"data/users.json",
JSON.stringify(users,null,2)
);


res.json({
message:"Password reset successful"
});

};

module.exports={
sendOTP,
verifyOTP,
resetPassword
};
```

---

## routes/authRoutes.js

```js
const express=require("express");

const router=express.Router();

const {
sendOTP,
verifyOTP,
resetPassword
}=require(
"../controllers/authController"
);

router.post(
"/send-otp",
sendOTP
);

router.post(
"/verify-otp",
verifyOTP
);

router.post(
"/reset-password",
resetPassword
);

module.exports=router;
```

---

## TESTING IN POSTMAN

1. Send OTP

POST

```bash
http://localhost:3000/api/auth/send-otp
```

Body:

```json
{
"email":"keerthi@gmail.com"
}
```

---

2. Verify OTP

POST

```bash
http://localhost:3000/api/auth/verify-otp
```

Body:

```json
{
"email":"keerthi@gmail.com",
"otp":"123456"
}
```

---

3. Reset Password

POST

```bash
http://localhost:3000/api/auth/reset-password
```

Body:

```json
{
"email":"keerthi@gmail.com",
"newPassword":"9999"
}
```

---

## INTERVIEW QUESTIONS

1. What is OTP?
   → One Time Password

2. Why use OTP?
   → Additional security

3. What is Nodemailer?
   → Library for sending emails

4. Why hash passwords?
   → Security

5. Why use App Password?
   → Safer than real Gmail password

6. What happens after OTP verification?
   → User can reset password

---

## COMMON ERRORS

1. Email not sent
   → Wrong app password

2. User not found
   → Email missing

3. Invalid OTP
   → Wrong OTP entered

4. Password reset failed
   → User object missing

---

## DAY 10 SUMMARY

✔ Email sending
✔ OTP generation
✔ OTP verification
✔ Password reset flow
✔ Nodemailer integration

👉 Now your backend supports real password recovery 🔥

---

## NEXT STEP

DAY 11:
Socket.IO
Real-time Chat Application
--------------------------
