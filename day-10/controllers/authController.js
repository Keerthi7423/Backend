const fs = require("fs"); const bcrypt = require("bcryptjs"); const nodemailer = require("nodemailer"); // SEND OTP 
const sendOTP = async (req,res)=>{ const {email}=req.body; const data=fs.readFileSync( "data/users.json", "utf-8" ); let users=JSON.parse(data); const user=users.find( u=>u.email===email ); if(!user){ return res.status(404).json({ message:"User not found" }); } const otp=Math.floor( 100000+Math.random()*900000 ); user.otp=otp; fs.writeFileSync( "data/users.json", JSON.stringify(users,null,2) ); const transporter= nodemailer.createTransport({ service:"gmail", auth:{ user:process.env.EMAIL_USER, pass:process.env.EMAIL_PASS } }); await transporter.sendMail({ from:process.env.EMAIL_USER, to:email, subject:"OTP Verification", text:`Your OTP is ${otp}` }); res.json({ message:"OTP sent successfully" }); }; 
// VERIFY OTP 
const verifyOTP=(req,res)=>{ const {email,otp}=req.body; const data=fs.readFileSync( "data/users.json", "utf-8" ); const users=JSON.parse(data); const user=users.find( u=>u.email===email ); if(user.otp!=otp){ return res.status(400).json({ message:"Invalid OTP" }); } res.json({ message:"OTP verified" }); }; 
// RESET PASSWORD 
const resetPassword=async(req,res)=>{ const { email, newPassword }=req.body; const data=fs.readFileSync( "data/users.json", "utf-8" ); let users=JSON.parse(data); const user=users.find( u=>u.email===email ); const hashedPassword= await bcrypt.hash( newPassword, 10 ); user.password= hashedPassword; delete user.otp; fs.writeFileSync( "data/users.json", JSON.stringify(users,null,2) ); res.json({ message:"Password reset successful" }); }; module.exports={ sendOTP, verifyOTP, resetPassword };