DAY 6 — MONGODB + MONGOOSE (REAL DATABASE INTEGRATION)

---

## 🎯 GOAL

* Move from JSON file storage → real database
* Learn MongoDB basics
* Connect Node.js app to MongoDB
* Use Mongoose
* Perform CRUD with database

---

## 🧠 WHY MOVE FROM JSON TO DATABASE?

Problems with JSON file storage:

❌ Not scalable
❌ Data can be corrupted
❌ Hard for multiple users
❌ No proper querying
❌ Not used in real production apps

Solution:
→ Use MongoDB database

---

## WHAT IS MONGODB?

MongoDB = NoSQL database

Stores data as:

Collections → like tables
Documents → like rows

Example document:

{
"name": "Laptop",
"price": 50000
}

---

## WHAT IS MONGOOSE?

Mongoose = ODM (Object Data Modeling)

Used to:

* Connect MongoDB
* Create schema
* Validate data
* Perform CRUD operations

---

## 📦 INSTALL PACKAGES

npm init -y

npm install express mongoose

---

## 📁 PROJECT STRUCTURE

day-6-products-api/
│
├── config/
│   └── db.js
│
├── models/
│   └── Product.js
│
├── controllers/
│   └── productController.js
│
├── routes/
│   └── productRoutes.js
│
├── server.js
└── package.json

---

1. config/db.js

---

Purpose:
Connect MongoDB database

Code:

const mongoose = require("mongoose");

const connectDB = async () => {
try {
await mongoose.connect(process.env.MONGO_URI);
console.log("MongoDB Connected ✅");
} catch (error) {
console.log("DB Error:", error);
}
};

module.exports = connectDB;

---

2. models/Product.js

---

Purpose:
Define product structure

Code:

const mongoose = require("mongoose");

const productSchema = new mongoose.Schema(
{
name: {
type: String,
required: true
},

price: {
type: Number,
required: true
}
},
{
timestamps: true
}
);

module.exports = mongoose.model("Product", productSchema);

---

## SCHEMA EXPLANATION

name:
Stores product name

price:
Stores product price

required:
Field must be provided

timestamps:
Automatically adds:

* createdAt
* updatedAt

---

3. controllers/productController.js

---

Purpose:
Business logic

Functions:

GET ALL PRODUCTS

const products = await Product.find();

---

GET SINGLE PRODUCT

const product = await Product.findById(req.params.id);

---

ADD PRODUCT

const product = new Product(req.body);
await product.save();

---

UPDATE PRODUCT

await Product.findByIdAndUpdate(
req.params.id,
req.body,
{ new: true }
);

---

DELETE PRODUCT

await Product.findByIdAndDelete(req.params.id);

---

4. routes/productRoutes.js

---

Purpose:
Map URLs → controller functions

Routes:

GET /products

GET /products/:id

POST /products

PUT /products/:id

DELETE /products/:id

---

5. server.js

---

Purpose:
Main entry point

Code:

const express = require("express");
const connectDB = require("./config/db");
const productRoutes = require("./routes/productRoutes");

require("dotenv").config();

const app = express();

app.use(express.json());

connectDB();

app.use("/api", productRoutes);

app.listen(3000, () => {
console.log("Server running on port 3000");
});

---

6. .env FILE

---

MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/productsDB

Used to store:

* Database URL
* Secrets

---

## CRUD FLOW

Client Request
↓

Routes

↓

Controller

↓

Model

↓

MongoDB

↓

Response

---

## HOW TO TEST

GET:
http://localhost:3000/api/products

---

POST:
http://localhost:3000/api/products

Body:

{
"name":"Laptop",
"price":50000
}

---

PUT:
http://localhost:3000/api/products/:id

---

DELETE:
http://localhost:3000/api/products/:id

---

## COMMON ERRORS

1. ECONNREFUSED
   → MongoDB not running

2. Invalid scheme
   → Wrong MongoDB URL

3. Cannot find module
   → Install package

4. Validation failed
   → Missing required fields

---

## INTERVIEW QUESTIONS

1. What is MongoDB?
   → NoSQL database storing data as documents

2. What is Mongoose?
   → ODM for MongoDB

3. What is schema?
   → Blueprint of data structure

4. What is model?
   → Used to interact with collection

5. Difference between SQL and MongoDB?
   → SQL = tables
   → MongoDB = collections/documents

6. What is find()?
   → Fetch all documents

7. What is save()?
   → Store data in DB

8. What is findByIdAndUpdate()?
   → Update document

9. Why use database over JSON?
   → Scalability and reliability

10. What is .env?
    → Stores sensitive configuration

---

## DAY 6 SUMMARY

✔ Real database integration
✔ MongoDB Atlas setup
✔ Mongoose schema
✔ CRUD APIs
✔ Environment variables

👉 You moved from beginner storage → real backend database 🔥

---

## NEXT STEP

DAY 7:
Authentication
JWT
bcrypt
Protected Routes
----------------
